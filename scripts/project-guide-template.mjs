// Editable document source. All graphs are rendered from captured, real sandbox runs.
export function projectGuide({ runs, capturedAt, screenshots, sourceCommit }) {
  const esc = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const names = {'lost-response':'Lost delivery response','duplicate-event':'Duplicate delivery event','rate-limit':'Rate-limited delivery','invalid-response':'Unexpected payment response'};
  const date = new Date(capturedAt).toISOString().slice(0,10);
  const svg = (height, body) => `<svg viewBox="0 0 660 ${height}" xmlns="http://www.w3.org/2000/svg" role="img"><defs><marker id="arrow" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L0,6 L6,3z" fill="#648873"/></marker></defs>${body}</svg>`;
  const box = (x,y,w,h,title,sub,dark=false) => `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="10" fill="${dark?'#244936':'#edf3e9'}" stroke="#cbdacb"/><text x="${x+w/2}" y="${y+29}" text-anchor="middle" fill="${dark?'#fff':'#243b2e'}" font-size="15" font-weight="600">${title}</text><text x="${x+w/2}" y="${y+51}" text-anchor="middle" fill="${dark?'#dce9d7':'#596d5b'}" font-size="11">${sub}</text>`;
  const arrow = (x1,y1,x2,y2) => `<path d="M${x1},${y1} L${x2},${y2}" stroke="#648873" stroke-width="2" marker-end="url(#arrow)"/>`;
  const architecture = svg(500, `${box(10,15,180,70,'Browser','UI + workspace cookie')}${box(240,15,180,70,'Vercel','Next.js server-side proxy')}${arrow(190,50,240,50)}${box(240,137,180,70,'VPS runner','TypeScript + real HTTP',true)}${arrow(330,85,330,137)}${box(475,137,175,70,'SQLite','Completed run reports')}${arrow(420,172,475,172)}<text x="345" y="116" font-size="11" fill="#596d5b">Authenticated HTTPS</text>${box(10,285,180,70,'Inventory','Loopback HTTP :4311')}${box(240,285,180,70,'Payment','Loopback HTTP :4312')}${box(475,285,175,70,'WSO2 API Manager','Private HTTPS + OAuth',true)}<path d="M330,207 V244 H100 V285 M330,244 V285 M330,244 H562 V285" fill="none" stroke="#648873" stroke-width="2" marker-end="url(#arrow)"/>${box(475,412,175,70,'Delivery','Loopback HTTP :4313')}${arrow(562,355,562,412)}<text x="14" y="420" font-size="14" font-weight="600" fill="#244936">One VPS. Deliberate boundaries.</text><text x="14" y="445" font-size="12" fill="#596d5b">The three providers share one Node process.</text><text x="14" y="467" font-size="12" fill="#596d5b">Only delivery passes through WSO2.</text>`);
  const sequence = svg(355, `<text x="110" y="25" text-anchor="middle" font-size="14" font-weight="600">Runner</text><text x="505" y="25" text-anchor="middle" font-size="14" font-weight="600">Delivery provider</text><path d="M110,45 V345 M505,45 V345" stroke="#bfd0bd" stroke-dasharray="5 5"/>${arrow(110,76,505,76)}<text x="245" y="63" font-size="12">Book delivery · key A</text>${box(430,93,155,65,'Committed','Ledger now contains A',true)}<rect x="24" y="178" width="240" height="40" rx="6" fill="#faeee5"/><text x="36" y="203" font-size="12" fill="#a55433">150 ms: caller stops waiting</text><text x="356" y="202" font-size="12" fill="#596d5b">Reply delayed for 650 ms</text>${arrow(110,253,505,253)}<text x="229" y="241" font-size="12">Retry: key B or key A?</text><text x="66" y="315" font-size="13" fill="#a55433">New key B: a second booking</text><text x="351" y="315" font-size="13" fill="#276b4e">Same key A: existing booking</text>`);
  const coverDiagram = svg(145, `${box(8,32,175,72,'RESERVE','Inventory')}${box(244,32,175,72,'AUTHORIZE','Payment')}${box(485,32,167,72,'BOOK','Delivery',true)}${arrow(183,68,244,68)}${arrow(419,68,485,68)}`);
  const charts = Object.entries(names).map(([id,name]) => {
    const pair = ['baseline','recovery'].map(strategy => runs.find(r=>r.scenario===id&&r.strategy===strategy));
    return `<div class="chartgroup"><h3>${name}</h3>${pair.map((r,i)=>`<div class="barrow"><span>${i?'Recovery':'Baseline'}</span><div class="bartrack"><i style="width:${r.checks.filter(c=>c.passed).length/4*100}%;background:${i?'#287969':'#b16c48'}"></i></div><b>${r.checks.filter(c=>c.passed).length}/4</b></div>`).join('')}</div>`;
  }).join('');
  const pages=[];
  const page = (label,title,subtitle,body,cls='') => pages.push(`<section class="page ${cls}"><header><b>rehearsal.</b><span>PROJECT GUIDE / ${label}</span></header><div class="section-label">${String(pages.length).padStart(2,'0')} / ${label}</div><h1>${title}</h1><p class="lead">${subtitle}</p>${body}<footer><span>Tharinda.dev · Rehearsal</span><span>${String(pages.length+1).padStart(2,'0')} / 13</span></footer></section>`);
  pages.push(`<section class="page cover"><header><b>rehearsal.</b><span>ENGINEERING FIELD NOTES</span></header><div class="edition">PROJECT DOCUMENTATION / ${date}</div><h1>Small failures.<br/>Better<br/><em>decisions.</em></h1><p class="cover-lead">A visual guide to API failures, recovery strategies<br/>and the evidence behind one order.</p><div class="cover-flow">${coverDiagram}</div><div class="cover-bottom"><div><span>DESIGN & DEVELOPMENT</span><strong>Tharinda.dev</strong></div><div><span>READ IT. RUN IT. QUESTION IT.</span><p>Real HTTP · WSO2 API Manager · Observable outcomes</p></div></div><footer><span>Rehearsal / Professional project report</span><span>01 / 13</span></footer></section>`);
  page('START HERE','The project in one minute.','A successful response is useful. A correct business outcome is what matters.',`<div class="callout"><h2>Did the operation fail, or did we just lose its reply?</h2><p>Rehearsal makes that question visible. It sends a sandbox order through inventory, payment and delivery, introduces one controlled failure, and checks what actually committed.</p></div><div class="grid3"><article><b>01 / Observe</b><p>Run a deliberately naive baseline. Watch the request trace and failed checks.</p></article><article><b>02 / Compare</b><p>Repeat the fault with a safer decision. Each experiment gets a fresh ledger.</p></article><article><b>03 / Explain</b><p>Read the effects, compare strategies and download evidence as PDF or JSON.</p></article></div><h2>Find what you need</h2><div class="contents">${[['3','Your first experiment'],['4','Why a timeout is ambiguous'],['5','Hosted system architecture'],['6','Source structure & responsibilities'],['7','Four failures, four recovery lessons'],['8','Observed results, visualized'],['9','The WSO2 gateway boundary'],['10','Data, privacy & persistence'],['11','Interface & evidence exports'],['12','Development & deployment'],['13','Verification & next steps'],['14','Glossary & references']].map(([n,t])=>`<div><span>${t}</span><b>${n}</b></div>`).join('')}</div><p class="note">For curious users, students and technical reviewers. No real payments, deliveries or customer data are involved.</p>`);
  page('QUICK START','Run your first rehearsal.','You can start in the hosted studio. No installation is needed.',`<figure><img src="${screenshots.studio}" alt="Rehearsal studio showing the refreshed interface"/><figcaption>Figure 1. Actual local interface capture; hosted experiments use the same controls. Transport labels identify the execution mode.</figcaption></figure><div class="steps">${[['Choose a failure','Leave Lost delivery response selected for your first attempt.'],['Run Baseline','The naive retry uses a fresh key. Inspect the delivery count, not just HTTP status.'],['Run with recovery','Reuse the original operation key in a new, isolated experiment.'],['Compare and export','Compare strategies shows the latest pair. PDF is for reading; JSON is for inspection.']].map(([t,d],i)=>`<div><b>${i+1}</b><p><strong>${t}</strong>${d}</p></div>`).join('')}</div><a class="link" href="https://rehearsal-kumuditha.vercel.app">Open the live studio → rehearsal-kumuditha.vercel.app</a>`);
  page('THE ENGINEERING PROBLEM','A timeout is not a rollback.','The caller and provider can disagree about what happened without either behaving mysteriously.',`<div class="diagram">${sequence}</div><div class="grid2"><article class="warm"><h2>Baseline: new key</h2><p>The provider treats the retry as another operation. One paid order can acquire two delivery bookings.</p><div class="large">A + B <small>two operations</small></div></article><article><h2>Recovery: same key</h2><p>The provider recognizes the previous operation and returns its existing result, instead of creating another.</p><div class="large">A + A <small>one operation</small></div></article></div><div class="callout"><strong>Idempotency, in plain English</strong><p>Repeating the same logical request has the same business effect. Here, the key is scoped to one provider and one run. This in-memory implementation does not prove exactly-once delivery across replicas.</p></div>`);
  page('ARCHITECTURE','Follow one request end to end.','The hosted version pairs a Vercel interface with an always-on DigitalOcean backend.',`<div class="diagram">${architecture}</div><div class="grid2"><article><h3>Why not everything on Vercel?</h3><p>The provider listeners and SQLite history need an always-on process and persistent disk. Vercel hosts the interface and authenticated proxy.</p></article><article><h3>Why only delivery through WSO2?</h3><p>One narrow, verified managed boundary is easier to explain. Inventory and payment remain direct HTTP; the runner remains TypeScript.</p></article></div><p class="note">Solid arrows show actual request or persistence paths. This is a single-node demo, not a high-availability topology. Ballerina is not in the live path.</p>`);
  page('PROJECT STRUCTURE','Small modules. Clear ownership.','Each part has one job, so a reader can follow a run without learning the whole repository first.',`<div class="code-tree"><b>Rehearsal/</b><br/>├── src/app/ <span>studio, layout and web API routes</span><br/>├── src/lib/<br/>│   ├── engine.ts <span>workflow decisions and business checks</span><br/>│   ├── gateway.ts <span>server-side OAuth and selected routing</span><br/>│   ├── store.ts <span>SQLite report persistence</span><br/>│   ├── report-pdf.ts <span>browser-compatible PDF export</span><br/>│   └── types.ts <span>run contract and failure presets</span><br/>├── sandbox/server.ts <span>three loopback HTTP providers</span><br/>├── vps-backend/ <span>authenticated backend and deployment units</span><br/>├── integrations/wso2/ <span>OpenAPI, provisioning, verification</span><br/>├── tests/ <span>engine, backend, OAuth, PDF and browser checks</span><br/>├── scripts/ <span>launch, live acceptance and guide generation</span><br/>├── docs/ <span>decisions, setup notes and captured evidence</span><br/>└── public/docs/ <span>downloadable project guide PDF</span></div><h2>The contract between layers</h2><table><tr><th>Boundary</th><th>What crosses it</th></tr><tr><td>Browser → web route</td><td>Scenario and strategy, never a user-selected target URL.</td></tr><tr><td>Runner → provider</td><td>X-Rehearsal-Run plus a stable operation key.</td></tr><tr><td>Backend → browser</td><td>Streamed events, then a completed Run report.</td></tr><tr><td>Report → PDF renderer</td><td>Already-visible run data. No backend secrets or new execution.</td></tr></table><p class="note">The structure reflects the source reviewed for this guide. Commit at evidence capture: <code>${esc(sourceCommit)}</code>.</p>`);
  page('SCENARIOS','Four faults. Different decisions.','Choose the recovery that matches the failure; “retry everything” is not a strategy.',`<div class="scenario-cards">${[
    ['01','Lost delivery response','Delivery commits, then delays its reply beyond the 150 ms caller deadline.','Reuse the same operation key. A retry resolves to the existing delivery.','The naive baseline can create two bookings.'],
    ['02','Duplicate delivery event','The runner deliberately replays the same logical delivery trigger.','Map both attempts to the same key so the provider suppresses a duplicate.','This is a replay inside the runner, not a webhook receiver or message broker.'],
    ['03','Rate-limited delivery','The sandbox rejects the first delivery attempt with 429 and Retry-After: 1.','Wait one second, then retry once with the same key.','This injected provider error is separate from the WSO2 quota probe.'],
    ['04','Unexpected payment response','Payment commits, but its response has the wrong field name.','Validate the response and reconcile against the payment ledger before continuing.','Do not charge again just because a response cannot be parsed.']
  ].map(([n,t,f,r,l])=>`<article><div class="scenario-title"><span>${n}</span><h2>${t}</h2></div><div class="grid2"><p><b>Failure</b>${f}</p><p><b>Recovery</b>${r}</p></div><small>${l}</small></article>`).join('')}</div>`);
  page('OBSERVED RESULTS','What changed when we recovered?','Eight real hosted runs: one baseline and one recovery for each failure preset.',`<div class="legend"><span class="orange-dot">Baseline</span><span class="green-dot">Recovery</span><span>Scale: 0–4 business checks passed</span></div>${charts}<h2>Delivery effects in the same runs</h2><table><tr><th>Scenario</th><th>Baseline bookings</th><th>Recovery bookings</th></tr>${Object.entries(names).map(([id,name])=>`<tr><td>${name}</td>${['baseline','recovery'].map(s=>`<td>${runs.find(r=>r.scenario===id&&r.strategy===s).ledger.deliveries}</td>`).join('')}</tr>`).join('')}</table><p class="note">Captured ${esc(capturedAt)} from the public app. All reports used gateway-configured transport. A failed baseline is an expected experimental outcome, not a failed test. These eight observations are not a reliability percentage or a production benchmark.</p><p class="small">Source data: docs/evidence/project-guide-runs.json. Full IDs, events, counts and checks are retained for reproduction.</p>`);
  page('WSO2 INTEGRATION','Security is a separate question.','A gateway can reject an unauthorized call. It cannot choose a safe business key for the caller.',`<div class="diagram">${svg(180,`${box(10,50,180,75,'Runtime application','OAuth client credentials')}${box(244,50,175,75,'WSO2 gateway','Authentication + subscription',true)}${box(475,50,175,75,'Delivery API','Sandbox side effects')}${arrow(190,88,244,88)}${arrow(419,88,475,88)}`)}</div><table><tr><th>Check</th><th>Observed response</th><th>Meaning</th></tr><tr><td>Missing token</td><td>401 / 900902</td><td>Gateway rejected unauthenticated access.</td></tr><tr><td>Invalid token</td><td>401 / 900901</td><td>Gateway rejected invalid credentials.</td></tr><tr><td>Valid token</td><td>200</td><td>Managed delivery health was reachable.</td></tr><tr><td>Separate quota probe</td><td>429 / 900800</td><td>WSO2 enforced its own request quota.</td></tr><tr><td>Provider quota</td><td>429 / Retry-After: 1</td><td>Injected sandbox response survived the gateway.</td></tr></table><div class="grid2"><article><h2>Normal delivery API</h2><code>/rehearsal/delivery/1.0.0</code><p>Execute, inspect the ledger and check health. Outer proxy and backend limits still apply.</p></article><article><h2>Read-only quota probe</h2><code>/rehearsal/gateway-probe/1.0.0</code><p>Two requests per minute. Kept separate so quota testing does not disrupt rehearsals.</p></article></div><p class="note">Policy evidence comes from the verified 7 September 2026 record in docs/VERIFICATION.md. API Manager 4.7.0 runs privately on the VPS. A green UI health indicator proves connectivity, not every security policy.</p>`);
  page('DATA & PRIVACY','Keep reports. Isolate experiments.','Provider state and completed reports have different lifetimes—and that distinction is intentional.',`<div class="diagram">${svg(290,`${box(10,25,190,75,'Workspace cookie','Opaque browser capability')}${box(260,25,175,75,'Backend query','Filter by workspace',true)}${arrow(200,62,260,62)}${box(260,178,175,75,'SQLite runs','Durable report JSON')}${arrow(345,100,345,178)}${box(10,178,190,75,'Provider ledger','Memory / per-run ID')}<text x="458" y="60" font-size="12" fill="#596d5b">Other workspace:</text><text x="458" y="83" font-size="12" fill="#596d5b">different history</text><text x="10" y="142" font-size="12" fill="#a55433">Resets with provider restart</text>` )}</div><h2>Actual SQLite table: runs</h2><table><tr><th>Column</th><th>Role</th></tr><tr><td>id · TEXT PRIMARY KEY</td><td>Unique completed run ID.</td></tr><tr><td>started · TEXT NOT NULL</td><td>Run timestamp; used to sort the latest reports.</td></tr><tr><td>body · TEXT NOT NULL</td><td>Serialized Run: events, ledger counts and business checks.</td></tr><tr><td>workspace · TEXT NOT NULL</td><td>Query boundary; defaults to local for local use.</td></tr></table><div class="grid2"><article><b>What survives</b><p>Completed SQLite reports survive backend restarts. The interface returns the newest 100 per workspace.</p></article><article class="warm"><b>What does not</b><p>Provider ledgers are in memory. Clearing the cookie loses access to that workspace; no account recovery exists.</p></article></div><p class="note">Cookies and runtime secrets are not included in documentation evidence. PDF export runs inside the browser using the report already loaded there. Downloaded reports are ordinary shareable files: review them before distributing.</p>`);
  page('INTERFACE & EXPORTS','Designed to make evidence legible.','The interface follows the experiment: configure, observe, verify, then explain.',`<figure><img src="${screenshots.compare}" alt="Comparison view with baseline and recovery checks and a ledger bar chart"/><figcaption>Figure 2. Actual local comparison view. Counts reflect its selected runs, not a decorative illustration.</figcaption></figure><div class="grid2"><article><h2>Run report PDF</h2><p>Readable summary, pass/fail checks, ledger chart, elapsed-event graph and the complete paginated trace. Original JSON is embedded as an attachment.</p></article><article><h2>Project guide PDF</h2><p>This illustrated document explains the system, code structure, deployment, evidence and limits. It is not a report for a particular visitor.</p></article></div><h2>Small UX decisions that matter</h2><div class="chips"><span>Visible PDF + JSON controls</span><span>Keyboard focus and dialog dismissal</span><span>Mobile layouts</span><span>Reduced-motion support</span><span>No fake progress metrics</span><span>Explicit developer credit</span></div><p class="note">Animations present observed events; they never change recorded execution times. PDF exports are not digitally signed certifications. JSON remains the machine-readable evidence format.</p>`);
  page('BUILD & OPERATE','From checkout to the hosted lab.','Use Node.js 24. Start locally before changing anything on a deployed server.',`<div class="grid2"><article class="dark"><h2>Local development</h2><pre>npm ci\nnpm run dev\n\n# Web: localhost:3040\n# Providers: 4311–4313</pre><p>Keep the terminal open. The launcher does not stop unrelated processes if a port is occupied.</p></article><article><h2>Verification commands</h2><pre>npm test\nnpm run typecheck\nnpm run test:e2e\nnpm run build</pre><p>Tests use controlled sandbox inputs. Browser checks can target the live app using PLAYWRIGHT_BASE_URL.</p></article></div><div class="steps"><div><b>1</b><p><strong>Web on Vercel</strong>Set BACKEND_URL, BACKEND_TOKEN and the expected app origin privately. Never use NEXT_PUBLIC_ for secrets.</p></div><div><b>2</b><p><strong>Backend on the VPS</strong>Node service as the rehearsal user. Nginx terminates public HTTPS. SQLite is outside the source release directory.</p></div><div><b>3</b><p><strong>Private WSO2 runtime</strong>Separate wso2 account, trusted localhost TLS and server-side client credentials. Public firewall permits only SSH, HTTP and HTTPS.</p></div><div><b>4</b><p><strong>Release and verify</strong>Deploy a reviewed archive, retain the old release, and verify real runs. Do not overwrite private credentials or the report database.</p></div></div><div class="callout"><b>Regenerating this guide</b><p>Run <code>node scripts/build-project-guide.mjs --capture</code> with the local studio running. It captures fresh public sandbox evidence and local screenshots. Without --capture it rebuilds from the retained assets. Review every rendered page before publishing.</p></div><p class="small">Detailed maintenance: docs/DEPLOYMENT.md and docs/WSO2.md. Private TLS renewal and off-server backup work are operational responsibilities, not features guaranteed by this PDF.</p>`);
  page('VERIFICATION & NEXT STEPS','Useful evidence. Honest limits.','A professional project report should make it easy to see both what works and what is unfinished.',`<div class="grid2"><article><h2>Verified in the project</h2><ul><li>Four baseline/recovery pairs over real HTTP.</li><li>Gateway authentication and separate quota enforcement.</li><li>Workspace separation and report persistence across a backend restart.</li><li>Automated engine, backend, OAuth and browser checks.</li></ul></article><article class="warm"><h2>Not claimed</h2><ul><li>Production throughput or reliability guarantees.</li><li>Durable provider deduplication across replicas.</li><li>Live Ballerina execution, team accounts or arbitrary API import.</li><li>Off-server restore, high availability or independent security certification.</li></ul></article></div><h2>A practical next iteration</h2><div class="roadmap"><div><b>01 / Make effects durable</b><p>Persist provider operation keys with a uniqueness constraint.</p></div><div><b>02 / Audit independently</b><p>Add and verify a typed Ballerina ledger auditor.</p></div><div><b>03 / Harden operations</b><p>Review subscriber privileges, backups, restore and certificate rotation.</p></div></div><p class="note">Each improvement above is a next step, not a claim about the current release. Keep acceptance results with the commit that was actually tested.</p>`);
  page('GLOSSARY & REFERENCES','Keep asking the right questions.','A short vocabulary for reading the project and discussing its engineering decisions.',`<h2>Glossary in plain English</h2><table><tr><td>API gateway</td><td>A managed checkpoint in front of an API.</td></tr><tr><td>Idempotency key</td><td>A stable identity for one logical operation.</td></tr><tr><td>Ledger</td><td>The provider's record of committed effects.</td></tr><tr><td>Reconciliation</td><td>Check the record when a reply is ambiguous.</td></tr></table><h2>Source and further reading</h2><div class="references"><a href="https://github.com/mr-kumuditha/rehearsal">Project source, README and verification record — GitHub</a><a href="https://apim.docs.wso2.com/en/latest/">WSO2 API Manager — official documentation</a><a href="https://pdf-lib.js.org/docs/api/classes/pdfdocument">PDF-LIB — PDF generation API</a></div><p class="note">Prepared for accessible project review, not as an endorsed WSO2 case study. Design and development credit: Tharinda.dev. Evidence capture: ${esc(capturedAt)}.</p>`);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"/><title>Rehearsal — Visual Project Guide</title><style>
  @page{size:A4;margin:0}*{box-sizing:border-box}body{margin:0;color:#25372d;background:#e6ebe3;font-family:'Guide Sans',Arial,sans-serif;font-size:14px;line-height:1.6}.page{width:794px;height:1123px;padding:48px 62px 80px;position:relative;background:#fbfcf8;break-after:page;overflow:hidden}.page:last-child{break-after:auto}header{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid #d9e3d5;padding-bottom:18px;margin-bottom:31px}header b{font-size:23px;letter-spacing:-1px}header span,.section-label{font-size:9px;letter-spacing:1.7px;font-weight:600;color:#56744e}.section-label{margin-bottom:10px}h1{font-size:34px;line-height:1.15;letter-spacing:-1.2px;margin:0 0 14px;font-weight:650}h2{font-size:20px;line-height:1.3;letter-spacing:-.5px;margin:23px 0 13px}h3{font-size:14px;margin:0 0 8px}p{margin:0 0 13px}.lead{font-size:15px;color:#5c7058;margin-bottom:25px;line-height:1.65}.callout{padding:22px 25px;background:#e9f1e3;border-left:4px solid #567b45;margin:23px 0}.callout h2{margin-top:0}.callout p:last-child,article p:last-child{margin-bottom:0}.grid2,.grid3{display:grid;gap:18px;margin:21px 0}.grid2{grid-template-columns:1fr 1fr}.grid3{grid-template-columns:repeat(3,1fr)}article{padding:21px;background:#f0f4eb;border:1px solid #dce6d5;border-radius:9px}article h2{margin:0 0 12px}article b{display:block;margin-bottom:9px}article p{font-size:13px}.warm{background:#fbf0e7;border-color:#ecdbcc}.dark{background:#263e30;color:#f0f7e7;border:0}.dark p{color:#d5e2cd}.contents{display:grid;grid-template-columns:1fr 1fr;gap:0 25px}.contents>div{display:flex;justify-content:space-between;padding:9px 0;border-bottom:1px solid #dfe6da;font-size:12px}.contents b{color:#42714e}.note{font-size:11px;line-height:1.7;color:#5b6e56;margin-top:20px;border-top:1px solid #dfe6da;padding-top:13px}.small{font-size:10px;color:#62725b}.link{display:block;color:#27653e;font-size:12px;font-weight:600}footer{position:absolute;bottom:30px;left:62px;right:62px;display:flex;justify-content:space-between;font-size:9px;color:#5d7355;border-top:1px solid #d8e3d0;padding-top:13px}figure{margin:20px 0}figure img{width:100%;max-height:420px;object-fit:contain;border:1px solid #d6e1d0;border-radius:8px;background:white}figcaption{font-size:10px;color:#5f7159;margin-top:8px}.steps>div{display:flex;gap:17px;margin:15px 0}.steps>div>b{border:1px solid #bfd3b5;border-radius:50%;width:29px;height:29px;flex-shrink:0;display:grid;place-items:center;color:#3c7345;font-size:12px}.steps strong{display:block;font-size:14px;margin-bottom:3px}.steps p{font-size:12px}.diagram{background:#fff;border:1px solid #dce5d5;border-radius:12px;padding:15px;margin:22px 0}.diagram svg{width:100%;height:auto;display:block;font-family:Arial,sans-serif;fill:#243b2e}.large{font-size:40px;color:#346541;font-weight:600}.warm .large{color:#a85c37}.large small{display:block;font-size:12px;font-weight:400}.code-tree{font-family:monospace;background:#253d2f;color:#e8f1e1;padding:24px;border-radius:10px;font-size:12px;line-height:2.05;white-space:nowrap}.code-tree span{color:#bdd0b4;font-size:10px}table{width:100%;border-collapse:collapse;font-size:12px;margin:18px 0}th{text-align:left;background:#e9f0e2;color:#3a5c32;font-size:10px;text-transform:uppercase;letter-spacing:.6px}td,th{padding:10px 12px;border-bottom:1px solid #dce5d5;vertical-align:top}td:first-child{font-weight:550}code{font-family:monospace;font-size:11px;overflow-wrap:anywhere}pre{font-size:13px;line-height:1.9;margin:10px 0 15px;white-space:pre-wrap}.scenario-cards article{margin:15px 0;padding:20px}.scenario-title{display:flex;gap:15px;align-items:center}.scenario-title span{font-size:11px;color:#567d49}.scenario-title h2{font-size:18px;margin:0}.scenario-cards .grid2{margin:12px 0 0}.scenario-cards b{font-size:10px;text-transform:uppercase;letter-spacing:1px;margin-bottom:4px}.scenario-cards small{font-size:10px;color:#627357}.legend{display:flex;gap:20px;font-size:10px;margin-bottom:18px}.legend span:before{content:'';display:inline-block;width:8px;height:8px;margin-right:6px}.orange-dot:before{background:#b16c48}.green-dot:before{background:#287969}.chartgroup{margin:16px 0 22px}.chartgroup h3{font-size:13px}.barrow{display:flex;align-items:center;gap:13px;margin:7px 0;font-size:11px}.barrow>span{width:65px}.bartrack{flex:1;height:15px;background:#e9eee3;border-radius:3px;overflow:hidden}.bartrack i{display:block;height:100%}.barrow>b{width:29px}.chips{display:flex;flex-wrap:wrap;gap:9px}.chips span{border:1px solid #d2dfca;border-radius:24px;padding:7px 12px;font-size:11px}ul{padding-left:16px;margin:0;font-size:12px}li{margin-bottom:8px}.roadmap{border-left:2px solid #9ab38b;padding-left:22px;margin:18px 0}.roadmap>div{margin:15px 0}.roadmap b{font-size:12px;color:#35673c}.roadmap p{font-size:12px}.references a{display:block;color:#2a663e;font-size:11px;margin:5px 0}.cover{background:#243a2d;color:#eff5e7}.cover header{border-color:#51634b}.cover header span{color:#cad9bd}.edition{font-size:10px;letter-spacing:2px;color:#c8d8b8;margin-top:58px}.cover h1{font-size:77px;line-height:1.05;letter-spacing:-3px;margin:28px 0 24px;font-weight:550}.cover em{font-style:normal;color:#c5dbae}.cover-lead{font-size:18px;color:#d5e1c9;line-height:1.7}.cover-flow{margin-top:50px;background:#f3f6ee;border-radius:12px;padding:17px 7px}.cover-flow svg{display:block;width:100%;font-family:Arial}.cover-bottom{display:flex;justify-content:space-between;margin-top:65px;gap:30px}.cover-bottom span{font-size:8px;letter-spacing:1.2px;color:#bdceb0;display:block}.cover-bottom strong{font-size:24px}.cover-bottom p{font-size:11px;margin-top:10px}.cover footer{color:#c6d6b8;border-color:#51634b}a{color:inherit}
  .page:nth-child(4) .diagram svg{max-height:270px}.page:nth-child(10) .diagram svg{max-height:235px}.page:nth-child(12) .steps>div{margin:7px 0}.page:nth-child(12) .grid2{margin:15px 0}.page:nth-child(12) article{padding:17px}.page:nth-child(12) .callout{padding:17px 21px;margin:15px 0}.page:nth-child(12) .lead{margin-bottom:5px}.page:nth-child(12){padding-bottom:60px}</style></head><body>${pages.join('').replaceAll('/ 13', '/ '+pages.length)}</body></html>`;
}
